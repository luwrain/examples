Плагин java-library в данном случае применять не совсем корректно, но он был использован ради java-library-distribution, который позволяет быстро упаковать получившееся расширение со всеми зависимостями.
luwrain.jar и luwrain-base.jar в compileOnly, потому что они входят в стандартную поставку luwrain и в упакованном дистрибутиве не нужны.
по той же причине, вероятно, в CompileOnly должны быть и все те библиотеки, которые входят в поставку luwrain.

строки, подключающие luwrain.jar и luwrain-base.jar из maven, пока по понятной причине закомментированы.
перед сборкой надо взять luwrain.jar и luwrain-base.jar из дистрибутива luwrain и поместить их в каталог lib в корне проекта.